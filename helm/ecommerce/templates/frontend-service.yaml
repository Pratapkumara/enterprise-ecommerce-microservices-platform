apiVersion: v1
kind: Service

metadata:
  name: ecommerce-frontend
  labels:
    app: ecommerce-frontend

spec:
  type: ClusterIP

  selector:
    app: ecommerce-frontend

  ports:
    - name: http
      port: {{ .Values.services.frontend.port }}
      targetPort: http
      protocol: TCP
