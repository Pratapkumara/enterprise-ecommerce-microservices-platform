apiVersion: v1
kind: Service

metadata:
  name: user-service
  labels:
    monitoring: enabled

spec:

  selector:
    app: user-service

  ports:

  - port: {{ .Values.services.user.port }}
    name: http

    targetPort: {{ .Values.services.user.port }}

  type: ClusterIP
