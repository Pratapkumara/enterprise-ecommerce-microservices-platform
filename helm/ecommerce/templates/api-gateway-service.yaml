apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  labels:
    monitoring: enabled

spec:

  selector:
    app: api-gateway

  ports:

  - port: {{ .Values.services.apiGateway.port }}
    name: http

    targetPort: {{ .Values.services.apiGateway.port }}

  type: ClusterIP
