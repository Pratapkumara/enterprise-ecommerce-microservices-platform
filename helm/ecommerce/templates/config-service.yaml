apiVersion: v1
kind: Service

metadata:
  name: config-server
  labels:
    monitoring: enabled

spec:

  selector:
    app: config-server

  ports:

  - port: {{ .Values.services.config.port }}
    name: http

    targetPort: {{ .Values.services.config.port }}

  type: ClusterIP
