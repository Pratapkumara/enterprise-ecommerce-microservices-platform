apiVersion: v1
kind: Service

metadata:
  name: payment-service

spec:

  selector:
    app: payment-service

  ports:

  - port: {{ .Values.services.payment.port }}

    targetPort: {{ .Values.services.payment.port }}

  type: ClusterIP
