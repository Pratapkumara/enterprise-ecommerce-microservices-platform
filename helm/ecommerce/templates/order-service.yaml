apiVersion: v1
kind: Service

metadata:
  name: order-service

spec:

  selector:
    app: order-service

  ports:

  - port: {{ .Values.services.order.port }}

    targetPort: {{ .Values.services.order.port }}

  type: ClusterIP
