apiVersion: v1
kind: Service

metadata:
  name: product-service

spec:

  selector:
    app: product-service

  ports:

  - port: {{ .Values.services.product.port }}

    targetPort: {{ .Values.services.product.port }}

  type: ClusterIP
